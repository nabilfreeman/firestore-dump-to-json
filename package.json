{
  "name": "firebase-dumper",
  "version": "1.0.0",
  "description": "Dumps a firestore and converts it into a nested map similar to the browsable one in Firebase Console",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "bash run.sh"
  },
  "author": "@nabilfreeman github",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/firestore": "^5.0.2",
    "firestore-backfire": "^1.1.1"
  },
  "engines": {
    "node": "14"
  }
}
